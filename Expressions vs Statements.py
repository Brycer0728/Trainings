# expressions vs statements
# an expression is a piece of code that produces a value ie: 
# the 'iq / 5' would be the expression
iq = 100
user_age = iq / 5
# a statement is an entire line of code that performs an action: 'user_age = iq / 5' 'iq=100'